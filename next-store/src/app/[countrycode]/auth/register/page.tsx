import { RegisterForm } from "@/components/auth/register-template";

const RegisterPage = () => {
  return (
    <div className="bg-primary-foreground">
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
